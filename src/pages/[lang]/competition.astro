---
import CompetitionPage from '../../components/pages/CompetitionPage.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'kz' } }
  ];
}

const { lang } = Astro.params;
---
<CompetitionPage lang={lang} />
